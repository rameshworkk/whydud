# ============================================================
# Whydud — PRIMARY Node (12 GB / 6 CPU)
# PostgreSQL PRIMARY + Full App Stack
# ============================================================

services:

  postgres:
    image: timescale/timescaledb:latest-pg16
    container_name: whydud-postgres
    restart: unless-stopped
    ports:
      - "127.0.0.1:5432:5432"
      - "10.8.0.1:5432:5432"
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-whydud}
      POSTGRES_USER: ${POSTGRES_USER:-whydud}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?required}
      REPLICATOR_PASSWORD: ${REPLICATOR_PASSWORD:?required}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/postgres/init.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
      - ./docker/postgres/primary.conf:/etc/postgresql/custom.conf:ro
      - ./docker/postgres/pg_hba.conf:/etc/postgresql/pg_hba.conf:ro
      - ./docker/postgres/init-replication.sh:/docker-entrypoint-initdb.d/02-replication.sh:ro
    command: >
      postgres
      -c config_file=/etc/postgresql/custom.conf
      -c hba_file=/etc/postgresql/pg_hba.conf
    deploy:
      resources:
        limits:
          memory: 3G
    shm_size: '256mb'
    networks:
      - whydud
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-whydud} -d ${POSTGRES_DB:-whydud}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: whydud-redis
    restart: unless-stopped
    ports:
      - "127.0.0.1:6379:6379"
      - "10.8.0.1:6379:6379"
    command: >
      redis-server
      --appendonly yes
      --maxmemory 512mb
      --maxmemory-policy allkeys-lru
      --requirepass ${REDIS_PASSWORD:?required}
    volumes:
      - redis_data:/data
    deploy:
      resources:
        limits:
          memory: 768M
    networks:
      - whydud
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3

  meilisearch:
    image: getmeili/meilisearch:v1.7
    container_name: whydud-meilisearch
    restart: unless-stopped
    ports:
      - "127.0.0.1:7700:7700"
    environment:
      MEILI_MASTER_KEY: ${MEILISEARCH_MASTER_KEY:?required}
      MEILI_ENV: production
    volumes:
      - meilisearch_data:/meili_data
    deploy:
      resources:
        limits:
          memory: 1G
    networks:
      - whydud
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:7700/health"]
      interval: 15s
      timeout: 5s
      retries: 3

  caddy:
    image: caddy:2-alpine
    container_name: whydud-caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./docker/Caddyfile.cloudflare:/etc/caddy/Caddyfile:ro
      - ./docker/certs:/etc/caddy/certs:ro
      - caddy_data:/data
      - caddy_config:/config
    deploy:
      resources:
        limits:
          memory: 128M
    depends_on:
      - backend
      - frontend
    networks:
      - whydud

  backend:
    build:
      context: .
      dockerfile: docker/Dockerfiles/backend.Dockerfile
      target: production
    container_name: whydud-backend
    restart: unless-stopped
    command: >
      gunicorn whydud.wsgi:application
      --workers 3
      --worker-class gthread
      --threads 2
      --bind 0.0.0.0:8000
      --timeout 120
      --access-logfile -
    env_file: ../.env
    environment:
      DJANGO_SETTINGS_MODULE: whydud.settings.prod
      DATABASE_URL: postgres://${POSTGRES_USER:-whydud}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-whydud}
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379/0
      CELERY_BROKER_URL: redis://:${REDIS_PASSWORD}@redis:6379/0
      MEILISEARCH_URL: http://meilisearch:7700
      MEILISEARCH_MASTER_KEY: ${MEILISEARCH_MASTER_KEY}
      SENTRY_DSN: ${SENTRY_DSN:-}
      NODE_ROLE: primary
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      meilisearch:
        condition: service_healthy
    volumes:
      - media_files:/app/media
    deploy:
      resources:
        limits:
          memory: 2G
    networks:
      - whydud

  frontend:
    build:
      context: .
      dockerfile: docker/Dockerfiles/frontend.Dockerfile
      target: production
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-https://whydud.com}
        NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL:-https://whydud.com}
    container_name: whydud-frontend
    restart: unless-stopped
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-https://whydud.com}
      NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL:-https://whydud.com}
      INTERNAL_API_URL: http://backend:8000
    depends_on:
      - backend
    deploy:
      resources:
        limits:
          memory: 1G
    networks:
      - whydud

  celery-worker:
    build:
      context: .
      dockerfile: docker/Dockerfiles/backend.Dockerfile
      target: production
    container_name: whydud-celery-worker
    restart: unless-stopped
    command: >
      celery -A whydud worker
      --loglevel=info
      --concurrency=2
      --queues=default,scoring,alerts,email,scraping
      --hostname=primary@%h
    env_file: ../.env
    environment:
      DJANGO_SETTINGS_MODULE: whydud.settings.prod
      DATABASE_URL: postgres://${POSTGRES_USER:-whydud}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-whydud}
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379/0
      CELERY_BROKER_URL: redis://:${REDIS_PASSWORD}@redis:6379/0
      CELERY_RESULT_BACKEND: redis://:${REDIS_PASSWORD}@redis:6379/1
      MEILISEARCH_URL: http://meilisearch:7700
      MEILISEARCH_MASTER_KEY: ${MEILISEARCH_MASTER_KEY}
      DISCORD_WEBHOOK_URL: ${DISCORD_WEBHOOK_URL}
      SENTRY_DSN: ${SENTRY_DSN:-}
      NODE_ROLE: primary
    depends_on:
      - backend
    deploy:
      resources:
        limits:
          memory: 1G
    networks:
      - whydud

  flower:
    build:
      context: .
      dockerfile: docker/Dockerfiles/backend.Dockerfile
      target: production
    container_name: whydud-flower
    restart: unless-stopped
    command: >
      celery -A whydud flower
      --conf=whydud/flowerconfig.py
      --port=5555
      --max-tasks=50000
      --inspect-timeout=10000
      --state-save-interval=10000
      --purge-offline-workers=24
    ports:
      - "5555:5555"
    env_file: ../.env
    environment:
      DJANGO_SETTINGS_MODULE: whydud.settings.prod
      CELERY_BROKER_URL: redis://:${REDIS_PASSWORD}@redis:6379/0
      CELERY_RESULT_BACKEND: redis://:${REDIS_PASSWORD}@redis:6379/1
      FLOWER_BASIC_AUTH: ${FLOWER_BASIC_AUTH:-admin:admin}
      FLOWER_URL_PREFIX: ""
    depends_on:
      - redis
    deploy:
      resources:
        limits:
          memory: 256M
    networks:
      - whydud

  celery-beat:
    build:
      context: .
      dockerfile: docker/Dockerfiles/backend.Dockerfile
      target: production
    container_name: whydud-celery-beat
    restart: unless-stopped
    command: >
      celery -A whydud beat
      --loglevel=info
      --scheduler django_celery_beat.schedulers:DatabaseScheduler
    env_file: ../.env
    environment:
      DJANGO_SETTINGS_MODULE: whydud.settings.prod
      DATABASE_URL: postgres://${POSTGRES_USER:-whydud}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-whydud}
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379/0
      CELERY_BROKER_URL: redis://:${REDIS_PASSWORD}@redis:6379/0
      SENTRY_DSN: ${SENTRY_DSN:-}
    depends_on:
      - backend
    deploy:
      resources:
        limits:
          memory: 256M
    networks:
      - whydud

  # ── Backup sidecar (runs pg_dump every 6 hours via sleep loop) ────────────
  backup:
    image: timescale/timescaledb:latest-pg16
    container_name: whydud-backup
    restart: unless-stopped
    entrypoint: /bin/bash
    command: ["/scripts/backup-entrypoint.sh"]
    env_file: ../.env
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: "5432"
      POSTGRES_DB: ${POSTGRES_DB:-whydud}
      POSTGRES_USER: ${POSTGRES_USER:-whydud}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?required}
      DISCORD_WEBHOOK_URL: ${DISCORD_WEBHOOK_URL:-}
      RCLONE_S3_ENDPOINT: ${RCLONE_S3_ENDPOINT:-}
      RCLONE_S3_ACCESS_KEY: ${RCLONE_S3_ACCESS_KEY:-}
      RCLONE_S3_SECRET_KEY: ${RCLONE_S3_SECRET_KEY:-}
      BACKUP_S3_BUCKET: ${BACKUP_S3_BUCKET:-whydud-backups}
      BACKUP_INTERVAL_SECONDS: "21600"
      NODE_ROLE: primary
    volumes:
      - backup_data:/backups
      - ./backend/scripts/backup.sh:/scripts/backup.sh:ro
      - ./backend/scripts/backup-entrypoint.sh:/scripts/backup-entrypoint.sh:ro
    depends_on:
      postgres:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 512M
    networks:
      - whydud

volumes:
  postgres_data:
  redis_data:
  meilisearch_data:
  caddy_data:
  caddy_config:
  media_files:
  backup_data:

networks:
  whydud:
    driver: bridge
